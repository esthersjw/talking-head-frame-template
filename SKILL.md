---
name: talking-head-frame-template
description: 将原始竖屏口播剪成“自定义边框图 + 中央口播窗口 + 大标题 + 字幕”的 ChatCut 成片。用于用户提供无包装的口播视频及任意带中央留白的边框/海报图，希望完成 1.1 倍速、去气口、字幕、标题和双图层排版时。
---

# 口播边框模板

将用户自己的边框图转成透明前景框，并把经过清理的口播放进中间窗口，做成可继续在 ChatCut 编辑的竖屏成片。

## 输入与开始方式

入口必须采用“边框优先”的引导，不要一开始就要求用户同时提供全部素材。第一句话只提供两种选择：**使用默认的 Esther 蓝色纸张边框**，或**上传自己的边框图**。

### 默认边框与自定义边框

- 默认边框：`assets/esther-default-frame-no-hands-clean-overlay.png`。用户选择它时，沿用已确认的“蓝色背景 + 灰色纸张 + 黄色贴纸”排版与已确认的文字风格；默认图不含人物手部，中央窗口为透明区域，且两侧为连续的普通纸张边框，不保留原手部对应的白色残边或透明缺口。
- 自定义边框：请用户上传一张边框/海报图。先分析它的口播窗口、前景遮挡元素与标题/字幕安全区；输出绿幕范围预览，待用户确认后才做透明前景框和正式剪辑。

实际成片必须在 ChatCut 中由原始边框（或背景）与原始口播视频的独立图层构成。不得使用生图模型合成 Demo，也不得把它生成的画面当作可编辑时间线的替代品。

1. **先请用户选择边框来源。** 用户可直接说“用默认边框”；若上传自己的边框/海报图，用自然的引导语说明：我会先判断口播窗口、前景装饰和标题字幕安全区，再请你确认绿幕范围。
2. **分析边框后再索取口播。** 识别中央留白的形状、位置、比例及需要置于最上层的元素；确认可以作为口播窗口后，再请用户上传干净录制的原始口播视频。
3. **仅在素材已同时提供时跳过引导。** 若用户一次性已上传边框和口播，仍先报告边框分析结论，再开始导入和剪辑。
4. **收集专有名词。** 在开始前请用户补充常用的人名、品牌、产品、英文表达和固定栏目名；将其与常见误识别写入本 Skill 的[专有名词库](references/proper-nouns.md)。用户未提供时，不要凭猜测把普通词改成品牌名。

边框图片应有预留给口播画面的中央留白区域。不要默认沿用示例图，也不要把示例图中的文字、颜色或装饰复制到用户的新设计里。

默认做竖屏 9:16；若原视频或边框图明显不是竖屏，先确认交付比例。将两个素材导入同一个 ChatCut 项目，等待口播视频文字稿可用后再剪辑。

## 首次校准与复用模式

本 Skill 分为两种运行模式。开始时根据用户是否已有确认过的模板偏好来选择。

### 第一次：校准模式

第一次使用同一套边框/账号风格时，按流程逐项展示并确认，不把关键审美决定一次性替用户做完。确认点依次为：

1. 边框窗口分析与人物取景；
2. 1.1 倍速；
3. 去气口、去重复和语义删改；
4. 字幕字体、字号、底卡、位置与断句；
5. 专有名词库；
6. 标题文案、两行/对齐方式、位置和图层缩放；
7. 人工检查、修改归属与最终复检方式。

首次专有名词步骤先展示基础词库中的预设项，并明确询问：“这些是我预设的常用名词；你还有人名、品牌、产品或英文表达需要补充/改写吗？”用户确认后，再写入其模板偏好。

将首次确认的边框、取景、安全区、字幕样式、标题样式、词库、收尾方式整理为该用户的“已确认模板偏好”。不要把未经确认的猜测写成默认。

### 第二次起：复用模式

当用户明确说沿用已确认模板，或已有该用户的已确认模板偏好时，默认直接端到端完成：导入、口播清理、取景、边框、字幕、词库校正、标题和全片复检均不逐项等待确认。标题沿用已确认的视觉样式；未提供标题时，根据口播内容自动采用最贴合的一条标题并放入最终复检。

只在以下情况暂停询问：边框窗口不清晰、素材比例无法安全适配、出现词库外且不确定的专名、用户提供了新的视觉要求、或自动处理发现明显的语义/画面问题。其余情况直接交付可编辑成片供用户整体复看。

## 工作顺序

严格按以下顺序工作。口播剪辑会改变时长，因此在口播定稿前，不要落地字幕、标题或边框图层。

1. **建立素材层级。** 将口播视频放在底层视频轨；边框图处理完成后放到上方视频轨。全片始终至少保留这两个视觉图层。
   - 先判定素材类型：明确要求“完整画布/背景”的图，整张作为底层背景，不要抠空或额外保留边框；将**原始口播视频整体**作为上层画面，按原比例 `contain` 放进指定版面区域，不抠人物、不替换人物背景、不用生图模型重绘。存在“窗口 + 外框遮挡关系”的图，则按透明前景边框处理：口播在后，边框和装饰在前。
2. **口播基础调整。** 将口播片段播放速度设为 `1.1x`。
3. **去气口与整理音频。** 按两轮完成，避免只删掉“呃”却留下大量重复表达。
   - **第一轮：机械清理。** 删除 `呃、额` 等纯犹豫音；将明显长停顿压缩至约 0.4 秒，句间保留自然呼吸空间。
   - **第二轮：主动语义清理。** 从头到尾重读清理后的文字稿。删除无意义的“然后、就是、比如说、啊、嗯、我想说、或者说”等口癖和重复连接词；删除同一句中的重起、卡住的半句、纠正前的失败表达，以及不增加新信息的重复句。优先保留一次最完整、最自然的说法。
   - **判断标准。** “然后、就是、所以、对”等词若承担顺序、转折、因果或必要语气则保留；若删掉后语义和口语节奏仍完整，则删除。不要把不同重录的半句硬拼成一句，不要为了时长删掉论点、例子或结论。每一轮后都重新读取文字稿，确认相邻句仍连贯。
   - **主线优先的主动删减。** 用户要求更紧凑时，允许删除完整语义块，而不只删口癖：删掉不推进主线的背景铺垫、重复解释、失败重说、例外或假设分支、剪辑元话语和情绪性填充。相同意思只保留最完整的一次；自我纠正时保留最终说法。保留核心观点、必要解释、至少一个有作用的例子、关键限定和结论。目标结构为“核心观点 → 必要解释 → 结论”；删后若跳义，只补回最短的必要连接。
4. **确认口播定稿。** 检查每个剪点的语义、连贯性和听感；确认后才继续做画面和字幕。若用户没有要求端到端自动完成，在此处请其确认。
5. **制作透明边框。** 先分析边框图片的中央留白形状。删除/抠掉中央留白，使该区域完全透明，保留外围背景、纸张边缘、手、贴纸和其它装饰。不要仅把中间盖成一种颜色：必须能透出下方口播画面。必要时轻微羽化内缘以消除锯齿，但不要吃掉边框。
   - **先给绿幕范围确认。** 对任何需要制作透明前景框的自定义图，先输出一张绿幕位置预览：绿色只表示即将透明、供口播透出的区域；边缘、夹子、星星、手、贴纸等需要遮挡口播的元素必须明确标为并保留在前景。窗口有圆角、弧线或不规则轮廓时，绿幕和最终 Alpha 必须沿真实轮廓绘制，绝不可退化为直角矩形。若用户要求不留纸边，透明区须扩到纸张的外轮廓内，不得留下白色内页边。先让用户确认“绿幕区域是否正确、哪些装饰必须压在口播上方”，再制作真正带 Alpha 的蒙版并放进 ChatCut。未经确认，不得直接用猜测的矩形透明区覆盖画面。
6. **适配口播窗口。** 以透明窗口的内边界为准裁切/缩放底层口播，始终锁定原始长宽比（使用等比 `contain` / 等比缩放，不得分别手动改宽、高来拉伸画面）。人物需居中且给发顶留出余量：完整头部、双肩与自然说话时的手势都必须可见；宁可再缩小一点、露出部分环境，也不要切掉发顶、肩膀或手。画面不可溢出边框内缘；若比例不合适，缩小并调整位置或做等比裁切，绝不拉伸人脸或背景。
   - **按边框自适配，不沿用固定坐标。** 每换一张边框，都重新测量透明窗口的实际宽高比、内边界和可用安全区；不要把上一张边框的口播尺寸或位置直接复制过来。窗口接近方形时按方形安全区适配，窗口偏竖/偏横时按实际比例适配。
   - **发顶安全边距优先。** 先以“发顶到上边框留出可见空隙”为验收条件，再确定人物大小和垂直位置；默认保留约窗口高度的 3–6% 作为上方安全边距。若原素材本身已贴近上沿，缩小整段等比画面并略向下移，不要向上裁切或拉伸补救。
   - **避免缩小后的黑边。** 缩小后若透明窗口露出底层空画布，先在同一窗口下铺与边框协调的背景/模糊延展底图，或在满足发顶余量的前提下回调到不露黑边的最大等比尺寸；不要为了消除黑边重新压掉发顶。开头、中段、结尾都抽帧检查人物比例、头顶留白与窗口边缘。
7. **标题提案、确认与排版。** 将透明边框全程覆盖在口播上方。添加两类文字：口播字幕放在窗口下方约 10–15% 的区域、但仍在视频画面内；顶部/底部大标题放在窗口外的留白区域，避开贴纸和手。
   - 先根据最终口播内容提出 3 个可选标题，至少覆盖“信息概括”“共鸣/观点”“好奇/行动”三个角度；默认控制在 **6 个汉字以内**（英文产品名按视觉宽度折算）、一行优先、字重醒目，且不从示例成片照搬文案。用户选用信息更完整的长标题时，优先拆成两行，而不是缩小到难读。
   - 将标题提案交给用户选择。用户可以直接选一个、要求混改，或提供自拟标题；在得到其明确选择前，不要把任何候选标题写入时间线。
   - 用户确认后再落地标题，并避开贴纸、手和主体。标题图层必须提供可编辑的标题文字、字体、字号、文字颜色、描边颜色/粗细、文字对齐、对齐宽度、水平位置和垂直位置；标题文本应保留用户输入的换行（直接回车即可分行）。两行标题默认左对齐，令两行共享同一左边缘；用户可在属性中切换为居中或右对齐。字幕每行短、易读，并避开人物的嘴部和手势。
8. **字幕样式、断句与专有名词终校。** 先按用户要求完成字体、字号、颜色/描边、单行规则及语义断句；再执行专有名词终校。
   - 以[专有名词库](references/proper-nouns.md)中的标准写法为准，逐项搜索最终口播文字稿和字幕。ASR 识别错的人名、品牌或英文短语，应先修正源文字稿；仅为显示需要的微调可使用字幕显示覆盖。
   - 多词英文专名必须保持完整拼写、大小写与空格，例如 `Vibe Coding`、`learn in public`。它们是不可拆分的语义单元：若一行容不下，应在短语之前或之后断句，不得把短语从中间拆开。
   - **完整词组检查。** 单行字数上限不能优先于词义完整性。逐卡检查相邻字幕交界：不得把中文固定词、亲属称谓、常用搭配或专有名词拆开，例如 `爸妈`、`游泳圈`、`跳海小酒馆`、`非标人生`、`可迁移能力`。若词组刚好跨越卡片边界，应将完整词组移动到下一张字幕卡，或用字幕显示覆盖合并为完整词组；不要让前一张只留下“爸/游泳/喜”等残字，也不要为凑字数吃掉词首或词尾。
   - 任何词库替换都要结合上下文复核。仅当该处明确指向该专名时，才将同音词或近似词替换为标准名；不确定时向用户确认。
9. **逐帧核验与时长回收。** 在开头、中段、结尾各检查一次：透明窗口是否正确透出人像、边框是否处于上层、标题和字幕是否遮挡装饰或主体、剪点是否自然、字幕是否跟最终声音同步；并抽查每个词库词的拼写、大小写和完整断句。
   - 若用户在后续手动增删口播片段，必须重新读取时间线，并把边框、标题和字幕的覆盖时长与最终口播画面核对。
   - 标题、边框、字幕与口播的结束帧必须一致；不得让大标题或任何可见文字在正文结束后单独停留。若标题是独立 Motion Graphic，应按最终口播时长新建或替换对应时长的图层，而不是保留旧的超长图层。
   - 口播画面结束后不得留下“透明边框 + 黑色中央窗口”的尾巴。默认在最后一个口播画面处收片；若用户希望保留尾段，先确认并改为明确的结尾卡、定格或其它设计化收尾。
10. **人工检查与最终复核（必做收尾）。** 自动处理完成后，不应直接视为成片；必须把可编辑预览交给用户进行一次人工检查，并用简短、可操作的引导收集反馈。
   - 先请用户依次看开头、中段、结尾，并明确提示其优先检查：剪辑是否删过头或仍有重复、人物是否被边框切到、标题/字幕的位置与大小、字幕错字/断句/专名、以及结尾是否完整。
   - 请用户把问题按“时间点或具体句子 + 想怎么改”说出来。示例：`01:12，把“Vibe Coding”改为标准拼写`、`字幕再往上一点`、`这一句删掉`。用户不必掌握剪辑术语。
   - 用户可明确选择“我自己在 ChatCut 调整”或“你直接帮我调整”。前者只提供对应操作路径与检查清单，不覆盖其手动结果；后者仅修改其点名或确认过的问题，并保留其它已确认的视觉设置。
   - **用户手动修改后的回读规则：** 用户说已删减、移动、改字或改样式后，先重新读取当前时间线、字幕与图层，再处理新问题。不得重建整段口播、重新生成全量字幕，或恢复任何被用户删掉的片段。
   - 用户手动删掉的完整语义块代表其对信息密度和取舍的确认：只检查删后是否断义、是否缺少必要上下文，不回填被删的背景、旁支、重复解释或情绪性填充。
   - 完成修改后，必须再做一次最终复核：按最终时间线检查开头/中段/结尾，确认字幕与声音同步、断句自然、专有名词正确、标题和字幕没有遮挡主体、所有图层覆盖到正确时长。将发现的问题直接修正；若没有问题，再交付“可导出的最终版”。

## 边框图处理规则

中央窗口可能是矩形、圆角矩形、纸张形状或不规则形状。按实际可见内边界建立透明区域；不要用画布中心或图片边缘猜测窗口尺寸。

对于类似“蓝色背景 + 中间纸张 + 两侧手 + 贴纸”的设计：移除的是纸张的内部填充，不是整张纸的轮廓。保留纸张白边、两侧手部、角落贴纸及外部背景，让它们在上层压住口播画面，形成“人物在纸张里说话”的效果。

若已明确去除两侧手部，则同时清除手部原先造成的白色残边、凹口或透明缺口；应将两侧整理为连续、自然的普通纸张边框。不要把手部遮挡关系原封不动带入无手版本。

如果图片没有明确的中央留白、中央装饰无法安全扣除，或裁切会损坏关键图案，暂停并请用户提供一张适合作为边框的图片或指出窗口范围。不要擅自生成全新的风格图替代用户素材。

## 成片默认样式

参照此模板的视觉关系，而非固定素材：

- 画布：9:16 竖屏。
- 层级：底层为口播，上层为透明边框；字幕和标题置于最上层。
- 主体：口播人物以等比缩放置入中央窗口，脸部位于窗口的上半至中部；验收时必须看见完整发顶、双肩与自然手势，且人物与背景没有被横向或纵向拉伸。
- 字幕：默认使用 **Smiley Sans**、白字、无描边、深色半透明圆角底卡（约 67% 不透明度；左右/上下内边距约 `12/5`、圆角约 `16`）；单行、每行最多约 11 个字符。字幕块默认宽约画面 85%，位于窗口下方的留白区；以用户当前项目设置为准，允许覆盖此默认值。
- 大标题：置于窗口外的留白区域，底部优先；使用与边框相配的高对比粗体。默认采用白字黑描边；标题较长时允许两行、左对齐，并保留用户输入的显式换行（例如 `How to` 换行后为 `用AI做个人网站`）。若底部没有安全留白，则改放顶部。

### 本模板标题基准（当前确认版）

- 两行标题放在底部蓝色留白区，左对齐；字幕位于标题上方，两者保留清晰间距。
- 优先等比放大**标题图层**来获得醒目的视觉字号，而不是单独把组件字号放得过大；这样文字、描边与行距会同步保持比例。
- 对 1080×1920 画布，本次确认的主段标题图层约为 `1782×297`，位置约为 `left=-220, top=1431`。这是此边框的视觉基准，不是对其他边框的固定坐标；应根据实际底部留白重新适配。
- 若标题因时间线切分为多个连续片段，复制同一图层的尺寸、位置和属性，确保全片不发生跳动。

详见 [references/layout-spec.md](references/layout-spec.md) 中的参考比例与验收清单。

## 专有名词库维护

词库是项目可复用的“标准名 → 常见 ASR 写法 → 排版规则”清单，存放在 [references/proper-nouns.md](references/proper-nouns.md)。每次新项目按以下方式使用：

1. 先加载基础词库，再让用户补充本次视频的专名和别名。
2. 在口播清理完成后，修正源文字稿中的识别错误，避免后续字幕反复出现旧写法。
3. 完成字幕字体、字号和语义断句后，做最后一次全量词库扫描，检查每个专名的拼写、大小写、空格与是否被断在两张字幕卡之间。
4. 将用户确认的新专名、可靠别名和排版注意事项补回词库；不要把未经确认的推测作为全局替换规则。

## 交付标准

交付前确认：

- 口播为 `1.1x`。
- 没有影响理解的重复、口癖、口误或突兀长停顿。
- 主线清晰；核心观点、必要解释、关键限定、必要例子和结论仍然完整。
- 中央窗口透明，边框在口播上方，轨道中存在上下两层视觉素材。
- 口播画面保持原始比例；完整头部、肩膀和自然手势均在窗口内可见，不出现拉伸或意外裁掉发顶的情况。
- 字幕、标题均以最终剪辑时间线为准，内容完整、位置不遮挡主体。
- 标题图层可直接编辑字体、字号、颜色、描边与位置；长标题的换行在预览和导出中保持一致。
- 词库中的专有名词均采用标准写法；多词英文专名未被拆开、大小写和空格正确。
- 任意字幕卡之间不拆开固定词组；抽查“父母称谓、常用搭配、产品/地点名、句首词”四类交界，确认没有残字、漏字或不足 0.2 秒的不可读闪现。
- 画面、声音、文字在开头、中段、结尾均经预览检查。
