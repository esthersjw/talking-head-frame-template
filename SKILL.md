---
name: talking-head-frame-template
description: 将原始竖屏口播剪成“自定义边框图 + 中央口播窗口 + 大标题 + 字幕”的 ChatCut 成片。用于用户提供无包装的口播视频及任意带中央留白的边框/海报图，希望完成 1.1 倍速、美颜、去气口、字幕、标题和双图层排版时。
---

# 口播边框模板

将用户自己的边框图转成透明前景框，并把经过清理的口播放进中间窗口，做成可继续在 ChatCut 编辑的竖屏成片。

## 输入与开始方式

第一步先确认两项素材已经提供：

- 原始口播视频：干净录制、未添加字幕或装饰。
- 边框图片：用户自己的设计图；中央必须是预留给口播画面的留白区域。

缺任何一项时，先请用户上传该素材。不要默认沿用示例图，也不要把示例图中的文字、颜色或装饰复制到用户的新设计里。

默认做竖屏 9:16；若原视频或边框图明显不是竖屏，先确认交付比例。将两个素材导入同一个 ChatCut 项目，等待口播视频文字稿可用后再剪辑。

## 工作顺序

严格按以下顺序工作。口播剪辑会改变时长，因此在口播定稿前，不要落地字幕、标题或边框图层。

1. **建立素材层级。** 将口播视频放在底层视频轨；边框图处理完成后放到上方视频轨。全片始终至少保留这两个视觉图层。
2. **口播基础调整。** 将口播片段播放速度设为 `1.1x`。应用轻度“小头”和“瘦脸”美颜：保持眼镜、发际线、鼻梁和背景直线自然；一旦出现变形，降低强度或关闭该项，不要为了明显效果牺牲真实感。
3. **去气口与整理音频。** 基于文字稿清理 `呃、额、啊、嗯` 等纯犹豫音、明显重复、口误重录、未说完的半句和无意义的长停顿。对于“然后、就是、所以、比如说、对”等词，只有在它确实是口癖或填充时才删除；承担顺序、转折、因果或自然语气时保留。选择完整自然的一次表达，不要把不同重录的半句硬拼成一句。长停顿压缩而非清零，句间保留约 0.3–0.5 秒呼吸空间。
4. **确认口播定稿。** 检查每个剪点的语义、连贯性和听感；确认后才继续做画面和字幕。若用户没有要求端到端自动完成，在此处请其确认。
5. **制作透明边框。** 先分析边框图片的中央留白形状。删除/抠掉中央留白，使该区域完全透明，保留外围背景、纸张边缘、手、贴纸和其它装饰。不要仅把中间盖成一种颜色：必须能透出下方口播画面。必要时轻微羽化内缘以消除锯齿，但不要吃掉边框。
6. **适配口播窗口。** 以透明窗口的内边界为准裁切/缩放底层口播，填满窗口并保持人物脸部居中。优先完整展示头部、肩部和手势；画面不可溢出边框内缘。若人像原始比例不合适，使用裁切和位置微调，而不是拉伸人脸。
7. **叠加与排版。** 将透明边框全程覆盖在口播上方。添加两类文字：口播字幕放在窗口下方约 10–15% 的区域、但仍在视频画面内；顶部/底部大标题放在窗口外的留白区域，避开贴纸和手。标题是内容概括，不要从示例成片照搬文案。标题优先两行以内、字重醒目；字幕每行短、易读，并避开人物的嘴部和手势。
8. **逐帧核验。** 在开头、中段、结尾各检查一次：透明窗口是否正确透出人像、边框是否处于上层、标题和字幕是否遮挡装饰或主体、剪点是否自然、字幕是否跟最终声音同步。

## 边框图处理规则

中央窗口可能是矩形、圆角矩形、纸张形状或不规则形状。按实际可见内边界建立透明区域；不要用画布中心或图片边缘猜测窗口尺寸。

对于类似“蓝色背景 + 中间纸张 + 两侧手 + 贴纸”的设计：移除的是纸张的内部填充，不是整张纸的轮廓。保留纸张白边、两侧手部、角落贴纸及外部背景，让它们在上层压住口播画面，形成“人物在纸张里说话”的效果。

如果图片没有明确的中央留白、中央装饰无法安全扣除，或裁切会损坏关键图案，暂停并请用户提供一张适合作为边框的图片或指出窗口范围。不要擅自生成全新的风格图替代用户素材。

## 成片默认样式

参照此模板的视觉关系，而非固定素材：

- 画布：9:16 竖屏。
- 层级：底层为口播，上层为透明边框；字幕和标题置于最上层。
- 主体：口播人物填充中央窗口，脸部位于窗口的上半至中部。
- 字幕：白字配高对比描边/阴影，位置略高于窗口底边，必要时移到窗口内较空的位置。
- 大标题：置于窗口外的留白区域，通常在底部；使用与边框相配但高对比的粗体字体。若底部没有安全留白，则改放顶部。

详见 [references/layout-spec.md](references/layout-spec.md) 中的参考比例与验收清单。

## 交付标准

交付前确认：

- 口播为 `1.1x`，美颜轻微且无明显变形。
- 没有影响理解的重复、口癖、口误或突兀长停顿。
- 中央窗口透明，边框在口播上方，轨道中存在上下两层视觉素材。
- 字幕、标题均以最终剪辑时间线为准，内容完整、位置不遮挡主体。
- 画面、声音、文字在开头、中段、结尾均经预览检查。
